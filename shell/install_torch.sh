git clone https://github.com/torch/distro.git ~/frameworks/torch --recursive
cd ~/frameworks/torch; bash install-deps;
./install.sh
# On Linux with bash
source ~/.bashrc
# On Linux with zsh
source ~/.zshrc
# On OSX or in Linux with none of the above.
source ~/.profile